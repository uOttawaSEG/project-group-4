<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.LoginTest">
        <activity
            android:name=".CalendarViewActivity"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> <!-- Add more activities as needed -->
        <activity android:name=".TutorInfoActivity" />
        <activity android:name=".StudentInfoActivity" />
        <activity android:name=".DashboardActivity" />
        <activity android:name=".AdminActivity" />
        <activity android:name=".LogOutPageActivity" />
        <activity android:name=".AdminInboxActivity" />
        <activity android:name=".TimeSlotActivity" />
        <activity android:name=".AvailableSessionListActivity"/>
        <activity android:name=".TutorInboxActivity"/>

    </application>

</manifest>